/* exported getLastCharacter */
function getLastCharacter(string: string): string {
  const lastIndex: number = string.length - 1;
  const lastCharacter: string = string[lastIndex];
  return lastCharacter;
}
